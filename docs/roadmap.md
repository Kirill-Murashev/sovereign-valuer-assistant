# Roadmap (Current Draft)

Completed:
- v0.1 repository skeleton.
- GigaChat smoke test path (`--smoke-llm`).
- report_review skill runner (`--run-skill report_review`).
- local RAG context for skill runs (`--use-rag`) with transparent source output.

Next:
- approved memory proposal workflow (without uncontrolled writes).
- first Data Layer tool (`get_risk_free_rate(date, maturity_years)`).
- more valuation skills.
